# 超出範圍知識庫

`.out-of-scope/` 目錄儲存被拒絕的功能請求的持久記錄。

## 目錄結構

每個概念一個檔案。多個請求相同東西的 issue 歸入一個檔案。

## 檔案格式

放鬆、可讀的風格。包含為何超出範圍的實質理由、先前請求清單。

## 何時檢查

分類期間（步驟 1），讀取所有 `.out-of-scope/*.md` 檔案。按概念相似度比對，非關鍵字。

## 何時寫入

僅在 enhancement（非 bug）被拒絕為 `wontfix` 時。不寫入已實作的功能。

## 更新或移除

若維護者改變主意，刪除 `.out-of-scope/` 檔案。
